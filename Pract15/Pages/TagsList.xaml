<Page x:Class="Pract15.Pages.TagsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pract15.Pages"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TagsList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Style="{DynamicResource Button}" Click="Back">Назад</Button>
            <Button Margin="5" Style="{DynamicResource Button}" Click="Add">Добавить</Button>
            <Button Margin="5" Style="{DynamicResource Button}" Click="Delete">Удалить</Button>
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding service.Tags}"
                  MouseDoubleClick="Edit"
                  SelectedItem="{Binding current}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                         <Run Text="Тэг:"/>
                         <Run Text="{Binding Name}"/>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
